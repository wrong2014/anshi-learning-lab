# P3 首次真实资料摄取记录（2026-06-30）

## 运行结论

P3 的官方资料摄取与任务树阶段已真实运行完成；多模型知识点生产尚未启动，原因是本地没有 `.env` 和真实 provider 配置。

## 官方课标摄取结果

| 学科 | PDF 页数 | 规范化 OCR 字符数 |
|---|---:|---:|
| 数学 | 189 | 113,890 |
| 小学科学 | 193 | 119,773 |
| 初中物理 | 71 | 40,688 |
| 初中化学 | 87 | 49,573 |
| 合计 | 540 | 323,924 |

检查结果：

- 4 份 PDF 均通过来源清单中的 SHA-256；
- 540 页全部有 OCR 输出；
- `(source_id, pdf_page)` 共 540 个，无重复；
- 中文字符间冗余空格已规范化，文本量从 608,940 字符降至 323,924 字符；
- OCR 采用 JSONL 页级检查点，中断后可续跑。

## 任务树结果

- 课程：4；
- 课标主题：27；
- 学段叶子任务：65；
- 总节点：96；
- 数学叶子：16；
- 小学科学叶子：39；
- 物理叶子：5；
- 化学叶子：5。

## 本地产物

- `artifacts/ocr/official-curriculum-2022.json`：完整 OCR 证据包；
- `artifacts/ocr/official-curriculum-2022.json.pages.jsonl`：页级断点；
- `artifacts/curriculum/k9-stem-seed.json`：确定性任务树；
- `artifacts/preview/k9-stem-task-tree.html`：本地交互预览；
- `artifacts/sources/*.pdf`：通过哈希核验的官方 PDF。

以上产物均位于 `.gitignore` 范围，不会把大型 PDF、OCR 和运行数据误提交到代码仓库。

## 尚未执行

以下步骤需要至少两个真实 Executor 模型和一个 Supervisor 模型：

1. 65 个叶子任务的双模型知识点提取；
2. 27 个主题内部关系生产；
3. 跨主题、跨学段、科学到物理/化学承接关系生产；
4. 人工抽检和黄金样本校准；
5. 首版不可变知识网络发布。

真实运行前需要创建非示例 `curriculum_providers.json`，并在环境变量中设置对应 API key。示例文件中的 endpoint 和 model 均为占位符，不能作为真实运行配置。

## 2026-07-01：真实双模型切片验收

已完成一至二年级数学 4 个任务叶子的真实生产切片，不是 mock 数据：

- 双候选模型：`deepseek-v4-flash`、`deepseek-v4-pro`；
- Supervisor：`deepseek-v4-pro`；
- 大纲节点：9；
- 原子知识点：54；
- 知识关系：97；
- 覆盖：4/4 `complete`；
- 关系类型：36 prerequisite、42 supports、3 progresses_to、7 transfers_to、
  2 bridges_to、7 parallel；
- 最终跨主题关系审查：93 分 `pass`；
- 独立 `validate-curriculum`：零错误。

生产过程中发现并修正了三类协议问题：

1. 冻结官方种子，禁止大纲智能体在已完成任务树下继续创造伪层级；
2. 引用跨页时只允许拆成单页原文，不允许拼接；高置信 OCR 引用校正需写入
   `curriculum_repairs` 审计表；
3. 有效候选按 provider 跨尝试保留，仍要求两个不同模型各有一份有效候选，
   不再要求它们必须碰巧在同一轮同时有效。

验收产物位于：

- `artifacts/pilot/math-g1-g2-network.json`
- `artifacts/pilot/factory-v5.db`

## 2026-07-01：第一层全量生产完成

第一层知识图谱全量生产与独立验收已完成，并发布不可变版本
`k9-stem-2022-v1.0.0`：

- 大纲节点：96；
- 叶子任务覆盖：65/65 complete；
- 原子知识点：719；
- 知识关系：1,386；
- 数学知识点：220；
- 小学科学知识点：323；
- 物理知识点：86；
- 化学知识点：90；
- 小学科学到物理 bridges_to：65；
- 小学科学到化学 bridges_to：145；
- 通过审查：99 次，最低 90 分，平均 94.94 分；
- 独立整网 validator：零错误；
- 自动化测试：24 项全部通过。

全量产物：

- `artifacts/curriculum/k9-stem-network.json`
- `artifacts/releases/curriculum-k9-stem-2022-v1.0.0.json`
- `artifacts/curriculum/factory-full.db`
